name: macOS

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Mac terminal
        run: |
          echo "🍎 Mac açıldı!"
          echo "------ Sistem ------"
          sw_vers
          system_profiler SPHardwareDataType

          echo "------ Bulunduğun klasör ------"
          pwd

          echo "------ Dosyalar (Finder gibi) ------"
          ls -la

          echo "------ Ana dizin ------"
          cd /
          ls

          echo "Terminal açık kalıyor..."
          sleep 3600
